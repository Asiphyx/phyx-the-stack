Original prompt: Iterate the Phyx the Stack opening/title screen so the music-reactive infinity visual feels smooth, clean, and immediately draws the player in.

## 2026-06-04

- Continuing from the title visualizer pass: the lemniscate conveyor is working, but the central title anchor reads too much like a heavy plaque.
- Current goal: integrate the title into the fold with a lighter signal-plate treatment, clearer front/back wrap, and less blocky flicker.
- Implemented signal-plate title treatment: transparent title glitch layers, single-line desktop wordmark, responsive mobile wrap, side connector latches, stronger reactive bars, and a lighter fold anchor.
- Verified with `npm run build`, the web-game Playwright client, and screenshots at `tmp_asset_contact/title-signal-plate-v2.png` and `tmp_asset_contact/title-signal-plate-mobile.png`.
- Pivoted the center back into a terminal-style plaque per feedback: visualizer layers now sit behind the plaque/save panel, bars are split equalizer strokes instead of one-sided spikes, and reverse-moving light nodes orbit the infinity path.
- Verified with `npm run build`, the web-game Playwright client, and screenshots at `tmp_asset_contact/title-terminal-plaque.png` and `tmp_asset_contact/title-terminal-plaque-mobile.png`.
- Fixed hero select name splitting by widening the showcase identity column and removing arbitrary word breaks from the selected hero name. Verified all six hero names render as one line at desktop width, with proof at `tmp_asset_contact/hero-select-asiphyx-name-fixed.png`.
- Replaced the bundled intro audio with `/home/asiphyx/Downloads/Downloads/C.A.I.T.mp3` at `public/assets/audio/cait-intro.mp3` and updated the title label to `Intro signal: C.A.I.T`.
- Reframed character select as a forced duo run: Cait is now the fixed Peon Queen companion, selectable heroes change her starting bond/module loadout, and run state stores `state.cait` for visible map/combat UI. Added `src/data/caitModules.js`, Cait HP/intent/module readouts, and `window.render_game_to_text`/`window.advanceTime` hooks for web-game verification.
- Corrected the title backdrop to use the actual generated Cait Labs art from `/home/asiphyx/Downloads/Downloads/new title screen.png`, copied to `public/assets/backgrounds/cait-labs-title.png`; the previous CSS-built imitation is now hidden behind the real asset.
- Converted the title launcher into a movable OS-style window: the top bar drags the pink HUD, CENTER recenters it, and the extra decorative text overlays were removed so the real Cait Labs backdrop owns the screen.
- Adjusted title backdrop from `cover` to exact viewport fitting so the INSOLENCE footer is not cropped by browser chrome/sidebar layouts, and strengthened the translucent pink CaitOS border/window chrome so New Run feels like launching Phyx from CaitOS.
- Cleaned up the forced duo hero-select screen: removed the duplicate bottom deploy strip, moved Start Duo Run into the selected-duo command panel, changed the giant portrait wells into square duo frames, compacted the Cait module stack so it fits, and converted the hero choices into a horizontal selector rail. Verified build plus browser screenshots at `tmp_asset_contact/hero-select-refined-final-browserlike.png` and `tmp_asset_contact/hero-select-refined-final-userlike-bindax.png`.
- Wired the first concept soundtrack pass: copied 12 active concept tracks into `public/assets/audio/soundtrack`, reserved `PERPETUAL MOTION TOAST-CAT.mp3` as `public/assets/audio/reserved/perpetual-motion-toast-cat-boss.mp3` for the future demo boss, added domain-aware score selection/filtering, and exposed beat/bass/mid/high CSS variables so title, hero select, map, combat, draft, and end surfaces react to both music and clicks. Verified with `npm run build`, browser phase checks through combat, and the web-game client output in `tmp_asset_contact/soundtrack-webgame-client`.
- Wired `/home/asiphyx/Downloads/Downloads/heroselect background.png` into its intended hero-select screen as `public/assets/backgrounds/hero-select-background.png`, with darker command-panel overlays so Cait's dream-cycle room reads behind the duo selector without sacrificing UI legibility.
- Completed the next HUD pass: title controls now open from the left Cait launcher, hero select uses the Cait backdrop as a positioned scene layer with the central eye aligned, combat removes the duplicate embedded Cait block, and Cait now has a draggable codec-style interaction window while cards/bottom console get extra vertical room. Verified with `npm run build`, browser screenshots at `tmp_asset_contact/title-cat-launcher-open-v2.png`, `tmp_asset_contact/hero-select-backdrop-integrated-v2.png`, `tmp_asset_contact/combat-codec-cleanup.png`, and the web-game client run in `tmp_asset_contact/hud-pass-webgame-client`.
- Corrected title layout feedback: moved the Phyx wordmark into the upper-left negative space, centered the launcher menu over the lower-left cat badge, centered the `Phyx Launch` label/buttons, and shortened the window status text to avoid clipping. Verified with `npm run build`, browser screenshot `tmp_asset_contact/title-left-wordmark-cat-align-v2.png`, and web-game client output in `tmp_asset_contact/title-left-wordmark-webgame-client`.
- Replaced the accidental baked-UI hero-select background with a clean scene-only Cait-o-Verse backdrop at `public/assets/backgrounds/hero-select-background-clean.png`, removed duplicate title backdrop framing, moved the title launcher/menu to a real lower-left hit area, enlarged the hero-select start action, and capped/reduced the title visualizer canvas work to lower render cost. Current architecture read: keep vanilla Vite for the jam, but plan a post-jam Phaser/Pixi renderer migration only if combat animation/audio timing grows past the current DOM+canvas renderer.

## 2026-06-11 — Cleanup, soundtrack rebrand, music player fix, asset forge

- Purged unreferenced assets: old hero versions (6/antigrav/asiphyx ×3 dirs), `mushroom-dream` + superseded `hero-select-background` backgrounds, `cards/agy*`, all of `cards/slices/`, vite-template `src/assets/`, empty `output/`, and 81MB of stale `tmp_asset_contact/` screenshots. Module raw gen sources moved from `public/assets/modules/` to `comfy/refs/modules/` (only the wired `cait-audio-modules-sheet.png` ships).
- Verified wiring: every static `/assets/*` reference and every dynamic `cardicon_<card.id>` resolves on disk; `npm run build` green.
- Rebranded all 12 soundtrack files to Cait-o-Verse names (git mv; e.g. `concept-ss` → `segfault-serenade`, `concept-track-7` → `phyx-anthem`) and rewrote `src/data/soundtrack.js` — domains and intensities unchanged. Old→new map in TODO.md.
- Music player fixes in `src/main.js`: removed double gain (element volume × WebAudio output gain), `ended` now simply advances to the next domain track, control bar updates in place instead of re-rendering the whole screen (slider drags survive, track name stays live), added ⏮/⏭ buttons.
- New hotkeys: `M` play/pause, `Shift+<`/`Shift+>` prev/next track (suppressed while typing in inputs).
- New `asset-guide/index.html` (18 copy-pastable image-gen prompt bricks with target paths) + `asset-guide/checklist.html` (agent-editable ASSET_STATUS JSON, todo→generated→cleaned→wired ladder). Working-state breadcrumbs live in `TODO.md`.
- Rebuilt hero select into focused hero spotlight pages instead of one cramped scroll stack: selected hero gets a command/lore panel, large duo feature art, readable passive/bond/mechanic/mode/module panels, previous/next page buttons, and a compact roster rail. Added per-hero spotlight/lore/mechanic/mode copy in `src/data/heroThemes.js`.
- Fixed title launcher dragging by retargeting the drag helper to the current `.title-cat-menu` instead of the removed `.title-fold-anchor`; verified the menu moves on the title screen.
- Moved music-reactive bounce off hero-select hit targets and onto decorative synth/stage layers. Desktop keeps the persistent music player with reserved rail space; mobile hides the hero-select music bar to avoid blocking roster taps.
- Verified with `npm run build` and local Playwright checks at 1600x768 and 430x932. Screenshots: `tmp_asset_contact/verify-hero-select-bindax-1600x768.png`, `tmp_asset_contact/verify-hero-select-bindax-430x932.png`, `tmp_asset_contact/verify-title-dragged-menu-1600x768.png`; both runs reported no console/page errors and `screenScrollable: false`.
- Follow-up correction: removed the bottom roster rail and the equal-weight Cait portrait from hero select. Hero select now flips pages with previous/next controls, keeps one selected hero in the spotlight, and shows Cait as a compact companion/codec signal in the details panel. Adjusted the title launcher anchor closer to the current background cat nose; if the title background asset changes, re-tune `.title-cat-launcher` percentages.
- Added the uploaded Cait Labs seal as `public/assets/brand/cait-labs-icon.png`, then wired it into the title launch window and the hero-select Cait companion signal. Rebalanced mobile hero-select rows so Start Duo Run and Cait's companion signal no longer clip. Re-ran `npm run build` plus 1600x768 and 430x932 Playwright hero-select/title-drag checks; build stayed green, title dragging worked, Bindax run state launched, both viewports were non-scrollable, and no console/page errors were reported.
- Corrected the title launcher nose alignment from the previous rough percentage to the measured central cat nose/mouth point in `cait-labs-title.png`: `.title-cat-launcher` is now `left: 49.9%; top: 52.4%`, and the button glow is centered at `50% 50%` instead of drifting upward. Verified with a fresh 1600x768 title screenshot at `tmp_asset_contact/title-nose-fixed-1600x768.png`, `npm run build`, and desktop/mobile Playwright checks.
- Centered the title visualizer orbit on the corrected cat centerline (`cx = 49.9%`) with the head-ring visual center at `cy = 43.5%`, and reduced the nose button override by roughly 20% to `58x46` at desktop max size. Verified with `tmp_asset_contact/title-nose-smaller-orbit-centered-1600x768.png`, `npm run build`, and 1600x768/430x932 Playwright regression checks.
- Reworked hero select into the "Void Crown Bond Chamber" direction: heading now says `CHOOSE VOID BOND`, the center card is enlarged into a glowing void altar with a selected-bond label beneath it, side panels are subdued glass terminals, the bottom synth/loading-bar look is reduced, and the CTA is treated as the ritual action. Fixed hidden mobile `scrollLeft` after hero page flips so the panels no longer clip left. Verified with `npm run build`, the existing Playwright hero-select regression at 1600x768 and 430x932, and default Asiphyx/Null Crown screenshot `tmp_asset_contact/void-crown-bond-chamber-asiphyx-1600x768.png`. The standard `develop-web-game` client script could not run because its ESM `playwright` dependency is not resolvable in this repo, so verification used the existing local Playwright script/global install path instead.

## 2026-06-12 — Budder Sphinx boss sprite processing

- Processed `/home/asiphyx/Downloads/reaction-and-other/needsprocessing.png` into Budder Sphinx assets with `scripts/process-budder-sphinx.mjs`, generating the main boss sprite, 4-frame idle strip, source sheet copy, animation frames, FX crops, and separated parts under `public/assets/enemies/budder_sphinx*`.
- Wired Budder Sphinx into `src/data/enemies.js` as a boss with the animated idle strip and supported combat intent pattern. Added the `assets:budder` npm script so the sheet can be regenerated from the downloaded source.
- Moved the reserved `Perpetual Motion Toast-Cat` track into the active soundtrack list on the new `boss` music domain, and taught combat rendering to use optional enemy idle sprite strips.
- Visual crop proof lives at `tmp_asset_contact/budder-sphinx-processed-contact-final.png`. Main and idle assets are clean; a few optional reference crops retain sheet labels where the source art touches labels, but those are not used by live combat.
- Verified with `npm run build` and seeded Playwright checks at 1600x768 and 430x932 using `tmp_asset_contact/verify-budder-boss.mjs`. Budder loads as `budder_sphinx_0`, uses `/assets/enemies/budder_sphinx_idle_strip.png`, and selects the `boss` soundtrack domain / `Perpetual Motion Toast-Cat` track with no console or page errors. Screenshots: `tmp_asset_contact/verify-budder-boss-1600x768.png` and `tmp_asset_contact/verify-budder-boss-430x932.png`.
- UI/image-gen follow-up: generated a 12-enemy pixel-art sprite-pack sheet with the built-in image-gen tool, extracted it into transparent cutouts under `public/assets/enemies/generated_pack/`, and rewired normal/elite/standard boss enemy sprites to those cutouts so combat enemies no longer read as card art. Budder Sphinx remains on the dedicated fixed boss sprite/idle strip.
- Combat HUD cleanup from screenshot feedback: menu now docks below the top bar without covering floor info, persistent music controls sit directly under the menu, Cait online/HP status moved into the draggable Cait codec, the stage header uses the clearer `spotlight` label, and enemy intent is a stable `Next enemy action` readout separated from the moving enemy sprite.
- Verified with `npm run build` and a seeded Playwright combat check through the global Playwright install (`NODE_PATH=/home/asiphyx/.local/share/fnm/node-versions/v22.22.1/installation/lib/node_modules`). Final proof: `tmp_asset_contact/verify-generated-enemy-combat-final-1792x1024.png`; result JSON confirms `/assets/enemies/generated_pack/memory_leak.png`, visible `Cait HP 92/92`, `menuOverlapsFloor: false`, and no console/page errors.
- Added the new tutorial-guide still as reusable local assets under `public/assets/tutorial/` plus a `TUTORIAL_GUIDE` data module. No tutorial route is implemented yet; hero select now shows her as a reserved guide signal so she is in the game surface without pretending the tutorial exists.
- Stored the three Cait-OS hero-select asset/reference packs under `public/assets/ui/cait-os/source-packs/` and wired the complete Cait-OS stage mock into hero select as `public/assets/backgrounds/cait-os-hero-select-stage.png`.
- Verified the asset-pack pass with `npm run build` and desktop/mobile Playwright screenshots through the global Playwright install. Proof: `tmp_asset_contact/verify-cait-os-pack-hero-select-final-1792x1024.png` and `tmp_asset_contact/verify-cait-os-pack-hero-select-final-430x932.png`; result JSON confirms the new stage background, visible guide card, no document scrolling, and no console/page errors.
- Cleaned `public/assets/enemies/` so it now holds only live runtime enemy sprites: generated cutouts, Budder's fixed static sprite, Budder's new roll strip, and the still-runtime Product Manager art. Moved old card-style enemy art and Budder sheet/crop/reference outputs into `public/assets/source/enemies/`.
- Regenerated Budder Sphinx's 4-frame roll animation with the built-in image-gen tool as `public/assets/enemies/budder_sphinx_roll_strip.png`. The generated frames keep the wings mounted on the back of the butter/toast rig through the roll, and `src/data/enemies.js` now uses that strip as Budder's `idleSprite`.
- Updated `scripts/process-budder-sphinx.mjs` so future sheet-crop processing writes to `public/assets/source/enemies/budder_sphinx_sheet_pack/generated/` instead of making the runtime enemy folder messy again.
- Verified with `npm run build` and a seeded Budder boss Playwright check. Proof: `tmp_asset_contact/verify-budder-roll-combat-1792x1024.png`; result JSON confirms the live animated sprite background is `/assets/enemies/budder_sphinx_roll_strip.png`, `background-size: 400% 100%`, and no console/page errors.

## 2026-06-17 — Asset review gallery

- Added `.superpowers/` to `.gitignore` because it is Claude scratch state, not Phyx source.
- Added `npm run assets:gallery`, which writes a browser-openable visual asset index to `tmp_asset_contact/asset-gallery.html`. The gallery groups images by folder, shows thumbnails, dimensions, alpha/file size, dirty/untracked git status, and exact source references where found.
- Moved the default `assets:contact` output to `tmp_asset_contact/asset-contact.png` and ignored `tmp/` so generated preview artifacts do not pollute git status.
- Generated a focused hero/background contact sheet at `tmp_asset_contact/demo-assets-contact.png`; this made the difference between clean scene backgrounds and baked-UI backgrounds obvious at a glance.
- Verified `npm run assets:gallery`, `npm run assets:contact -- public/assets/heroes public/assets/backgrounds --out tmp_asset_contact/demo-assets-contact.png`, and `npm run build`.
- Follow-up asset hygiene pass: embedded thumbnails directly into `assets:gallery` output so Desktop-opened HTML does not depend on browser file-path access, and added exact duplicate detection/filtering. Replaced the Race Condition runtime sprite with a new single unstable threaded-core enemy, cleaned Heisenbug cutout artifacts, re-centered Legacy Codebase, and sped up Budder Sphinx's 4-frame perpetual roll in both DOM and Phaser combat renderers. Proof sheet: `/home/asiphyx/Desktop/Phyx Enemy Sprite Review.png`. `npm run build` remains green.
- Locked Xadnib out of hero select while the Ultra Xadnib art remains unresolved. Replaced the live Budder Sphinx roll strip with a deterministic spin derived from the clean fixed sprite so both ears and the toast harness stay consistent across all four idle frames; the previous AI roll strip is preserved at `public/assets/source/enemies/budder_sphinx_roll/previous_ai_roll_strip.png`. Refreshed Desktop gallery/review sheets and verified `npm run build`.
- Revised the jam demo lock: all non-Cait hero pages remain visible in hero select, but only Asiphyx can launch; Codex, Xadnib, Bindax, and Antigrav show a disabled `W.I.P` CTA. The jam run now starts as an 11-floor route: 10 fight rounds with elites at floors 5 and 10, then Budder Sphinx as the fixed final boss. Runtime enemy instances now preserve `templateId`, `idleSprite`, and `idleFrames`, which fixes Budder's animated idle wiring in both DOM and Phaser paths. Verified with `npm run build`, a GameState smoke test for floor 11 Budder, and a Playwright browser proof at `tmp_asset_contact/jam-hero-select-proof.png`.
- Removed the extra floating Asiphyx mini-card overlay from hero select; the main hero art now renders without an injected shadow-leader badge. Restored the title visualizer anchor to the earlier cat-centered values (`cx = 49.9%`, `cy = 43.5%`) so the bars/orbit track the cat face instead of drifting toward the page. Verified with `npm run build` and screenshots at `tmp_asset_contact/asiphyx-no-floating-overlay.png` and `tmp_asset_contact/title-visualizer-recentered.png`.
- Tuned the title equalizer crown separately from the orbit glyphs: smaller right nudge, wider oval radius, and a slow glow opacity pulse. Recentered the triangle module glyph around its own centroid so its circular pulse/halo sits in the middle like the other module shapes. Verified with `npm run build`.
- Removed the duplicate portrait-side hero selector buttons from hero select; the info-panel prev/next controls are now the only selector surface. Verified assets/music on a fresh local server: Asiphyx and Codex portraits loaded at natural width 1254, Queen Circuit was active, non-Asiphyx CTA stayed disabled `W.I.P`, and proof screenshot was written to `tmp_asset_contact/hero-select-single-selector-proof.png`.
- Rebuilt Asiphyx's jam card identity around control mage/tank gravity support instead of direct attacks. His starter deck no longer includes `git_push`; Asiphyx-only rewards and injected debt are filtered so his run cannot receive direct-damage cards. Added `Cait Momentum` plus combat effects for queued extra Cait strikes and marked-target gravity crits (`mark_target_crit`, `cait_extra_action`). Reworked Asiphyx cards into Null Anchor, Vector Redirect, Center of Gravity, Cait Momentum, Kinetic Transmutation, Gravity Mirror, Mass Increase, Null Pointer Exception, and Event Vector.
- Added file-backed neon glyph card icons for every Asiphyx-specific card under `public/assets/cards/cardicon_*.png`, preserving the simple symbolic style while avoiding emoji fallback. Tightened combat card title wrapping so longer names like Center of Gravity and Cait Momentum fit in the narrow hand UI.
- Verified the Asiphyx card pass with `npm run build`, a GameState smoke test confirming no direct-damage deck/reward cards for Asiphyx, a deterministic combat test proving Center of Gravity + Cait Momentum resolves as an 11-damage Cait crit plus a 6-damage extra Cait strike, and browser proofs at `tmp_asset_contact/asiphyx-card-icons-contact.png`, `tmp_asset_contact/asiphyx-card-kit-browser-proof.png`, and `tmp_asset_contact/asiphyx-card-kit-forced-hand-fit.png`.
- Reworked the combat hand into a command-stack prototype: modules can be clicked or dragged into three staged slots, staging reserves projected energy without executing, filled slots can be clicked to unslot, and `SEND STACK` executes queued commands in order. Added a persistent battle log panel and changed the right-side turn button to `END TURN // RESOLVE ENEMY` so compiling commands is distinct from ending the turn. `render_game_to_text` now includes staged commands and battle-log lines for verification.
- Verified command-stack behavior with `npm run build` and browser automation: staging Center of Gravity kept energy at 3 and hand size at 5, unslotting cleared the queue, sending spent energy to 2 and removed the module from hand. Proof screenshots: `tmp_asset_contact/asiphyx-command-stack-staged.png` and `tmp_asset_contact/asiphyx-command-stack-sent.png`.
- Cleaned the jam vocabulary so the current slice no longer reads as a generic deckbuilder: README/meta copy now describes module stack tactics, Asiphyx's Cait loadout is `Kinetic Regent`, player-facing draft/stack labels say modules/stack, `TODO.md` is restored as the live breadcrumb, and legacy internal `CARDS`/`deck` identifiers are explicitly marked compatibility names. Verified with `npm run build` and a Vite dev-server Playwright smoke through title → Asiphyx launch → combat at `tmp_asset_contact/naming-cleanup-smoke.png`.

## 2026-06-18 — Music-folder UI asset combat HUD pass

- Copied the nine ChatGPT-labeled UI frame assets from `/home/asiphyx/Music` into `public/assets/ui/music/ui-01.png` through `ui-09.png`, then keyed out the baked near-white export background so the frames render with real transparency.
- Wired the assets into combat: Cait/Asiphyx battlefield presence stays at 35% opacity, socket bars use `ui-02`/`ui-08`, Cait codec uses `ui-05`, the bottom dashboard uses `ui-04`, and the new bottom-center module action tray uses `ui-02` plus `ui-09` card buttons.
- Added a bottom HUD module tray so playable modules read as larger named action buttons with `TO CAIT` / `TO TARGET` labels while existing battlefield sockets remain the drop targets.
- Verified with `npm run build` and a focused Playwright smoke at `tmp_asset_contact/verify-music-ui-combat.mjs`; proof screenshot is `tmp_asset_contact/music-ui-combat-hud-1792x1024.png` and result JSON confirms five module buttons, six sockets, no page errors, and resolved `/assets/ui/music/*.png` backgrounds.
- The standard `develop-web-game` client still cannot run in this repo because its ESM import cannot resolve the globally installed `playwright` package; the focused verifier uses `createRequire` plus `NODE_PATH` as the working local path.
- Follow-up correction after screenshot feedback: removed the player-facing HTML5/Phaser engine toggle and locked combat to Phaser as the arena renderer, leaving DOM only for the command HUD. Removed duplicated side module racks, subdued/moved socket rails, tightened the bottom command bay into one action surface, replaced the white Phaser hero portrait card with a small Asiphyx anchor sigil, and removed Phaser debug log text from the arena. Verified again with `npm run build` and `tmp_asset_contact/verify-music-ui-combat.mjs`; proof screenshot remains `tmp_asset_contact/music-ui-combat-hud-1792x1024.png`.
- Imported `/home/asiphyx/Music/canwe.zip` as Kinetic Regent Cait source animation material. Copied the original Aseprite exports into `public/assets/source/heroes/kinetic_regent_cait/`, added `scripts/process-cait-kinetic-regent.mjs` plus `npm run assets:cait-regent`, generated Cait-colored idle/walk sheets under `public/assets/heroes/kinetic-regent-cait/`, and wired the Asiphyx Cait codec to use the animated `cait_kinetic_regent_idle_strip.png`. Proof assets: `tmp_asset_contact/kinetic-regent-cait-idle-before-after.png` and `tmp_asset_contact/kinetic-regent-cait-combat-codec.png`.
- Added Budder Sphinx spacetime motion FX from Music-folder sprite sheets: copied `/home/asiphyx/Music/butter cat particles.png`, `bluestar sprite sheet.png`, and `purple .png` into `public/assets/enemies/fx/`, then wired Phaser combat to preload them only when Budder is present. Budder now renders with a green/pink vortex and orbiting star sheets behind the roll sprite to sell the "spin tipping spacetime" read. Also added a Budder-specific attack-vortex hook on damage, but the current combat re-render cycle can restart Phaser quickly enough that transient attack slashes are hard to capture; the persistent spacetime layer is the reliable visible improvement. Verified with `npm run build` and `tmp_asset_contact/verify-budder-spacetime.mjs`; proof screenshots: `tmp_asset_contact/budder-spacetime-combat-1792x1024.png` and `tmp_asset_contact/budder-spacetime-attack-1792x1024.png`. Result JSON confirms combat mode, floor 11, Budder Sphinx, boss music domain, nonblank Phaser canvas, and no console/page errors. Vite dev server started at `http://127.0.0.1:5173/` and left running.
- Corrected the Budder galaxy layer after visual review: kept the stars/orbiting motion and Budder float/roll, removed the grow/shrink pulse and whole-sprite rotation, and made the galaxy stay anchored behind him while the sheet frames animate in reverse for an internal counterclockwise spin. Rebuilt and reran `tmp_asset_contact/verify-budder-spacetime.mjs`; proof stayed clean with no console/page errors, and `tmp_asset_contact/budder-spacetime-combat-1792x1024.png` shows the anchored layer.
- Enlarged and sped up Budder's spacetime FX while preserving the corrected animation model: Budder's roll strip runs faster, the anchored reverse-frame galaxy is larger/faster, and the orbiting stars are larger with quicker arcs. Trimmed only galaxy alpha afterward so the bigger effect does not bury Budder's silhouette. Verified with `npm run build` and `tmp_asset_contact/verify-budder-spacetime.mjs`; proof remained clean with no console/page errors.
- Pushed the Budder boss FX larger again: galaxy sheet is now a much bigger anchored reverse-frame vortex, the orbiting star/planet bodies are larger and move on continuous clockwise orbit containers, each body also spins clockwise, and Budder's own sprite is nudged downward inside the warp. Verified with `npm run build` and `tmp_asset_contact/verify-budder-spacetime.mjs`; proof stayed clean with no console/page errors and `tmp_asset_contact/budder-spacetime-combat-1792x1024.png` shows the larger clipped-at-arena-edge spacetime effect.
- Refined that pass after screenshot feedback: reshaped the galaxy from over-tall/clipped to a wide arena-safe vortex, added the new Music-folder planet sheets (`planet.png`, `moon.png`, `blackplanet.png`) as additional clockwise orbit bodies, and changed the orbit math to a continuous 3D-ish ellipse with front/back scale and alpha instead of a flat framed sweep. Copied the two Asiphyx gravity-pull sheets into `public/assets/heroes/fx/` and wired Phaser combat to show teal/red gravity bands plus rings when Cait Momentum / gravity-Cait effects resolve. `Combat.js` is owned by `nobody:nogroup`, so the visual hook stays Phaser-side by reading combat state/toasts instead of editing engine ownership. Verified with `npm run build`, `tmp_asset_contact/verify-budder-spacetime.mjs`, and `tmp_asset_contact/verify-asiphyx-gravity-pull.mjs`; both proofs stayed clean with no console/page errors. Screenshots: `tmp_asset_contact/budder-spacetime-combat-1792x1024.png`, `tmp_asset_contact/asiphyx-gravity-pull-proof.png`.
- Toned down the over-opaque FX pass and desynchronized the visible loops: Budder's galaxy/vortex alphas are lower, orbiting stars/planets/moons use staggered frame starts, offsets, delays, and slightly different orbit/spin durations, and the Asiphyx gravity-pull bands are thinner/translucent instead of solid teal/magenta bars. Also stopped combat HUD clicks from restarting Phaser by only booting the Phaser combat scene once per combat phase and reattaching the persistent canvas on subsequent renders. Added a browser proof at `tmp_asset_contact/verify-phaser-no-click-reset.mjs`; result JSON confirms scene create count stayed `1` after target selection and module staging, with no console/page errors. Verified `npm run build`, `tmp_asset_contact/verify-budder-spacetime.mjs`, `tmp_asset_contact/verify-asiphyx-gravity-pull.mjs`, and `tmp_asset_contact/verify-phaser-no-click-reset.mjs`; proof screenshots include `tmp_asset_contact/budder-spacetime-combat-1792x1024.png`, `tmp_asset_contact/asiphyx-gravity-pull-proof.png`, and `tmp_asset_contact/phaser-no-click-reset-proof.png`.
- Reworked the opacity problem into a richer motion pass: Budder's source galaxy sheets now sit at much lower base alpha with breathing alpha/scale pulses, while the visual impact comes from front-layer glow cores, halo pulses, motes, and multiple orbit lanes. Blue/planet/black bodies keep the wider side orbit; purple star/moon use tighter vertical lanes in front of Budder so some FX spins up and around him instead of everything circling flatly behind him. Asiphyx's gravity pull was lowered again and supplemented with short-lived teal/magenta spark wakes along the pull vector. Verified with `npm run build`, `tmp_asset_contact/verify-budder-spacetime.mjs`, `tmp_asset_contact/verify-asiphyx-gravity-pull.mjs`, and `tmp_asset_contact/verify-phaser-no-click-reset.mjs`; all passed without console/page errors, and no-click-reset still reports scene create count `1`.
- Corrected the follow-up overcorrection where the FX became too transparent: raised Budder's additive galaxy pulse ceiling slightly, restored stronger orbit-body alpha, doubled down on visible glow cores/halos, increased corona strength, and raised particle-lane count from 22 to 34 with brighter front-layer motes. Verified `npm run build` and `tmp_asset_contact/verify-budder-spacetime.mjs`; proof screenshot `tmp_asset_contact/budder-spacetime-combat-1792x1024.png` shows a stronger seller-point effect without returning to the earlier opaque pasted-sheet look.
- Adjusted to the clarified art direction that Budder's FX should not be transparent, with roughly 10% transparency max for the important pieces. Main galaxy pulse now reaches near-solid opacity, orbit bodies use ~0.9+ alpha, glow halos/cores/corona are much brighter, particle motes are near-solid, and spacetime fracture lines are more visible. Verified with `npm run build` and `tmp_asset_contact/verify-budder-spacetime.mjs`; proof screenshot updated at `tmp_asset_contact/budder-spacetime-combat-1792x1024.png`.
- Rebuilt only the combat gameplay HUD around the new Gemini `stitch_fluid_thermal_core.zip` Technomancy mockup from `/home/asiphyx/Music`: copied `code.html`, `DESIGN.md`, and `screen.png` into `public/assets/ui/technomancy/source/`, then replaced the combat shell with a scoped `technomancy-combat` layout. Combat now uses a hard top OS strip, left Asiphyx operator rail, central magenta framed Phaser arena with initiative/socket overlays, right status report with segmented bars and recent events, and a bottom green combat-log/module terminal. Existing module staging, drag/drop sockets, send/clear/wait, right-side selected enemy status, Cait codec, and Phaser no-reset behavior remain wired. Added `tmp_asset_contact/verify-technomancy-combat-hud.mjs` for desktop/mobile proofs. Verified `npm run build`, `tmp_asset_contact/verify-technomancy-combat-hud.mjs` at 1792x1024 and 430x932, and `tmp_asset_contact/verify-phaser-no-click-reset.mjs`; all passed without console/page errors. Proof screenshots: `tmp_asset_contact/technomancy-combat-hud-1792x1024.png` and `tmp_asset_contact/technomancy-combat-hud-430x932.png`.
- Fixed Cait Momentum combat flow so the black hole now opens before Cait moves, pulls her attack vector toward the target, resolves the hit, then returns her momentum to source instead of showing an instant generic damage laser. Added a `caitAttackWindup` combat event, tagged Cait damage payloads for Phaser timing, delayed the enemy hit reaction until the pull animation lands, and guarded stale delayed combat DOM wiring so old renders cannot boot Phaser against a missing arena parent. Verified with `npm run build`, `tmp_asset_contact/verify-cait-momentum-black-hole.mjs`, `tmp_asset_contact/verify-phaser-no-click-reset.mjs`, and Technomancy HUD browser checks at 1792x1024 and 430x932; all passed without console/page errors. Proof screenshots: `tmp_asset_contact/cait-momentum-black-hole-windup.png`, `tmp_asset_contact/cait-momentum-black-hole-impact.png`, `tmp_asset_contact/technomancy-combat-hud-1792x1024.png`, and `tmp_asset_contact/technomancy-combat-hud-430x932.png`.
- Reworked combat flow away from energy-gated hand play and into module pathing: left rail now owns Asiphyx HP/ult, deck piles, user modules, Cait sync modules, stack controls, and wait; the arena keeps three Cait module slots plus three user path slots; the bottom bar is read-only Cait dialogue/lore/status with no module buttons. `Combat.playCard()` no longer checks/spends energy, Memory Leak no longer drains energy, and `render_game_to_text` reports user/Cait module caps instead of card costs. Removed the out-of-place arena `BATTLE STAGE`/theme header. Retuned Budder's spacetime bodies with more separated orbit/spin durations and replaced the stationary halo/core circle clusters with smaller scattered glitch sparks and shards. Verified with `npm run build`, `tmp_asset_contact/verify-technomancy-combat-hud.mjs` at 1792x1024 and 430x932, `tmp_asset_contact/verify-module-side-cap.mjs`, `tmp_asset_contact/verify-phaser-no-click-reset.mjs`, and `tmp_asset_contact/verify-budder-spacetime.mjs`; all passed without console/page errors. Proof screenshots include `tmp_asset_contact/technomancy-combat-hud-1792x1024.png`, `tmp_asset_contact/technomancy-combat-hud-430x932.png`, `tmp_asset_contact/module-side-cap-proof.png`, and `tmp_asset_contact/budder-spacetime-combat-1792x1024.png`.
- Follow-up HUD fit pass: moved the live hand module preview out of the cramped top strip and into the right Battle Notes rail as a `MODULE ROUTER`, moved the small draw/discard/void/hand/user/Cait counters into that same rail, and condensed the top strip back to identity + item/artifact/status chips. The left rail keeps SEND/CLEAR/ULT/END TURN actions. Hero select now places the Start Duo Run CTA as a heart/play control over Cait's chest core instead of consuming the lower-right info panel. Verified with scratch Vite build output and Playwright screenshots at `tmp_asset_contact/final-hero-select-verified-1920x865.png`, `tmp_asset_contact/final-combat-verified-1792x1024.png`, and `tmp_asset_contact/final-combat-mobile-verified-430x932.png`; no page or console errors.
- Combat balance/flow pass while waiting on final sprites: changed Kinetic Regent stack timing so routed modules resolve before Cait's payoff instead of Cait firing first and losing the target/momentum setup. Staged enemy commands now carry stable `targetId`s and refresh indices after deaths/summons so target locks do not drift. Gravity target modules now build Kinetic Combo without secretly adding extra attacks; `Cait Momentum` is the explicit extra-action module. The Cait Momentum black-hole payoff now uses orange/gold forward pull particles and blue return/reset wake, with a longer-lived visible rift for screenshots. Fixed two fight-system contract flaws found during testing: enemy `heal` intents are now real resolver actions instead of mislabeled strength buffs, and summon parsing strips trailing punctuation so `Summon a Null Pointer.` resolves correctly. Updated stale player-facing text for Memory Leak and Cait Momentum. Verification: direct engine smoke proved Center of Gravity + Cait Momentum resolves as module -> module -> Cait payoff -> enemy -> player, with Cait windups of 19 crit then 8 extra strike against a stable target; summon/heal smoke proved Null Pointer summon and TODO Comment repair; `npm run build` green; focused Playwright checks passed with no console/page errors: `tmp_asset_contact/verify-cait-momentum-black-hole.mjs`, `tmp_asset_contact/verify-phaser-no-click-reset.mjs`, `tmp_asset_contact/verify-technomancy-combat-hud.mjs` at 1792x1024 and 430x932, and `tmp_asset_contact/verify-asiphyx-gravity-pull.mjs`. Proof frames: `tmp_asset_contact/cait-momentum-black-hole-windup.png`, `tmp_asset_contact/cait-momentum-black-hole-impact.png`, `tmp_asset_contact/cait-momentum-black-hole-return.png`, `tmp_asset_contact/technomancy-combat-hud-1792x1024.png`, and `tmp_asset_contact/technomancy-combat-hud-430x932.png`. The standard `develop-web-game` client still fails here because its ESM import cannot resolve `playwright`; focused verifiers use global Playwright via `NODE_PATH`.
- Route balance/flaw-map follow-up: fixed the nested win-check bug where module or Cait auto kills could run `_combatWon()` twice and award double gold / double transition; `playCard()` and Cait auto turns now support deferred win checks while stack resolution owns the final combat-state transition. Fixed route pacing by making `FloorManager` prefer authored easy/medium/hard encounter groups instead of drawing early floors from the combined `normal` pool, which had allowed Memory Leak / Dependency Hell / Merge Conflict cliffs before the player had draft tools. Summons are now bounded to 3 active enemies, and summoned minions enter at 60% template HP with lane-full summons converting into caster block. Added a small Cait sync repair after non-final combats at 5% max HP, minimum 2, so the 11-fight jam route has breathing room without deleting boss danger. Auto-route sims moved from early-floor and summon drag failures into 34/40 victories with 6/40 losses, all losses at Budder; no pre-boss deaths remained in that no-draft stress path. Verification: engine smoke proved module-kill reward stays `12` gold, summon cap/light minion behavior works, and early floors draw only easy templates; `npm run build` passed; focused browser proofs passed without console/page errors for Cait Momentum black-hole FX, Phaser no-click-reset, and Technomancy combat HUD at 1792x1024 and 430x932. Visual proof inspected: `tmp_asset_contact/cait-momentum-black-hole-return.png` and `tmp_asset_contact/technomancy-combat-hud-430x932.png`.
- Stale-resource balance follow-up: found that Tech Debt and Budder still used old `maxEnergy` debuffs even though current combat pathing no longer spends energy, leaving elite/boss patterns with dead turns. Enemy `debuff` intents with a value now jam the next player draw by 1 module while keeping the old max-energy mutation as compatibility state; `startPlayerTurn()` consumes that jam, draws at least 1 module, and exposes `currentTurnDrawPenalty` through combat snapshots and `render_game_to_text`. Updated Tech Debt's `Compound` and Budder's `Biblical Head Roll` text to `Jam next draw by 1 module`, and surfaced active pressure as `DRAW JAM -1` in the combat top status/player notes/battle log so the player can see why the hand is smaller. Draft-aware route simulation with sensible deprecate/compile choices stayed in the intended shape after the change: 78/80 victories, 2/80 losses, no pre-boss deaths, average Budder fight 13.71 turns, and all losses at floor 11. Verification: focused engine smoke proved Tech Debt jam leaves the next hand at 1 module with `currentTurnDrawPenalty: 1`; browser proof `tmp_asset_contact/tech-debt-draw-jam-proof.png` shows the visible `DRAW JAM -1` chip/log and `render_game_to_text` reports the same state with no console/page errors; `npm run build`, Cait Momentum black-hole proof, Phaser no-click-reset, and Technomancy combat HUD desktop/mobile proofs all passed. The stock `develop-web-game` client remains blocked by its unresolved ESM `playwright` import, so focused Playwright verifiers using global `NODE_PATH` are still the working proof path.
- Draft reward-flow fix: confirmed `compile` rewards could still offer technical-debt/fault modules after fights (`deprecated_api` and `memory_leak` appeared 82 times across 200 compile offers), which made the reward terminal sometimes add cleanup work instead of a useful module. `DraftSystem` now filters debt rarity and curse-tagged modules out of compile offers while leaving `deprecate` able to remove existing stack debt. The draft reward UI also had a production-flow flaw: it reused compact 52px combat module buttons, making compile options tiny and barely readable; compile rewards now render as full draft module cards with icon, command lane, name, description, target path, rarity, and tags, with mobile top spacing so fixed music/menu controls do not cover the title. Verification: engine checks passed for `compile-no-debt` across 200 offers and `deprecate-still-removes-debt`; draft-aware route sim stayed boss-shaped at 78/80 victories, 2/80 losses, no pre-boss deaths; browser draft proofs passed on desktop and 430x932 with no debt/curse offers, readable card boxes, no horizontal overflow, and no console/page errors. Proof screenshots: `tmp_asset_contact/draft-compile-no-debt-proof.png` and `tmp_asset_contact/draft-compile-no-debt-430x932.png`. `npm run build`, Cait Momentum black-hole proof, Phaser no-click-reset, and Technomancy HUD desktop/mobile regressions passed; the stock `develop-web-game` client still fails on unresolved ESM `playwright`.
- Draw-jam state cleanup: removed the leftover `maxEnergy`/`energy` mutation from enemy `debuff` intents. The previous compatibility path made repeated Tech Debt/Budder draw jams secretly drive `maxEnergy` to 0 even though Asiphyx's current module-pathing fight flow does not spend energy; the visible draw jam was correct, but the hidden state drift was stale and misleading. Debuffs now only set the next-turn draw jam and leave energy state stable. Verification: focused engine smokes proved Tech Debt and four repeated Budder jam turns still produce `hand: 1` and `currentTurnDrawPenalty: 1` while preserving `energy/maxEnergy: 3/3`; draft-aware route sim stayed boss-shaped at 75/80 victories, 5/80 losses, no pre-boss deaths, and `energyDrift: 0`; browser proof `tmp_asset_contact/tech-debt-draw-jam-proof.png` confirmed visible `DRAW JAM -1`, hand size 1, `currentTurnDrawPenalty: 1`, and stable `energy/maxEnergy: 3/3` with no console/page errors. `npm run build`, Cait Momentum black-hole proof, Phaser no-click-reset, and Technomancy HUD mobile proof passed. The stock `develop-web-game` client was rerun and still fails on unresolved ESM `playwright`.
- Microservices reward cleanup: audited Asiphyx-legal compile rewards for stale cost/energy mechanics and found `Microservices` was still a dead cost-reduction reward (`All modules cost 1 less` / `reduce_all_costs`) after the move to module pathing. Reworked it into a current-system power: `At the start of each turn, draw 1 extra module` with a new `start_of_turn_draw` effect. Combat now stores `startOfTurnDraw`, applies it into the turn-start draw count alongside draw jams, surfaces `DRAW +1` in the HUD, and includes it in `render_game_to_text`. Verification: stale reward audit passed with zero Asiphyx compile rewards using dead cost/energy effects/text; focused engine smoke proved playing Microservices gives the next turn `hand: 3` and `startOfTurnDraw: 1`; draft-aware route sim stayed boss-shaped at 78/80 victories, 2/80 losses, no pre-boss deaths, `energyDrift: 0`, and Microservices was picked in 46/80 runs; browser proof `tmp_asset_contact/microservices-draw-throughput-proof.png` confirmed `DRAW +1`, hand size 3, `startOfTurnDraw: 1`, and no console/page errors. `npm run build`, Cait Momentum black-hole proof, Phaser no-click-reset, and Technomancy HUD desktop/mobile regressions passed. The stock `develop-web-game` client still fails on unresolved ESM `playwright`.
- Enemy strength damage-math fix: found a balance bug where enemy `strength` multiplied damage dealt *to* that enemy, so enrage/buff patterns made Memory Leak/Budder easier to kill instead of only increasing their outgoing threat. `_dealDamageToEnemy()` now applies incoming raw damage directly through block/HP and leaves strength scaling only on enemy outgoing attacks. Verification: repro before patch showed a 100-strength enemy taking 20 damage from a 10-damage hit; focused smokes after patch proved the same enemy takes exactly 10 incoming damage while a 100-strength 10-damage enemy attack still hits for 20 before Asiphyx's Gravitational Lens reduction. Draft-aware route sim stayed stable at 79/80 victories, 1/80 loss, no pre-boss deaths, boss-only loss, and `energyDrift: 0`. Browser proof `tmp_asset_contact/strength-incoming-damage-proof.png` confirmed Memory Leak at 40/50 after a 10-damage proof hit despite 100 strength, with no console/page errors. `npm run build`, Cait Momentum black-hole proof, Phaser no-click-reset, and Technomancy HUD desktop/mobile regressions passed. The stock `develop-web-game` client still fails on unresolved ESM `playwright`.
- Vector Redirect live-intent fix: found that `resolveModuleStack()` captured enemy intents before player modules resolved, so `swap_intent` could visibly change the enemy pattern while the old captured attack still fired in the same stack exchange. Enemy actions now refresh the live `enemy.pattern[patternIndex]` at execution time, and redirected attack/buff/debuff descriptions now say what the converted action actually does. Verification: focused engine smoke proved an Attack Probe 20-damage intent becomes `Redirected Guard` with 20 enemy block and no Asiphyx HP loss; draft-aware route sim stayed boss-shaped at 79/80 victories, 1/80 loss, no pre-boss deaths, and `energyDrift: 0`; browser proof `tmp_asset_contact/vector-redirect-live-intent-proof.png` shows the redirected guard state and matching tactical log with no console/page errors. `npm run build`, Cait Momentum black-hole proof, Phaser no-click-reset, and Technomancy HUD desktop/mobile regressions passed. The stock `develop-web-game` client was rerun and still fails on unresolved ESM `playwright`, so focused Playwright verifiers with global `NODE_PATH` remain the working proof path.
- Encounter metadata cleanup from balance-sim scan: the live `FloorManager` route was already using the 11-floor jam structure, but `src/data/enemies.js` still exported an old `getEncounterPool()` helper and `boss_1`/`boss_2`/`boss_3` pools from the previous 15-floor schedule. Updated the exported encounter metadata so floors 5/10 are elite, floor 11 is the Budder final boss, and invalid-floor errors use the actual run length. Verification: direct helper smoke proved floors 1/5/6/10/11 map to `easy`/`elite`/`medium`/`elite`/`boss`; current draft-aware route sim ran 80/80 victories with no pre-boss deaths and `energyDrift: 0`; `npm run build` stayed green.
- Compile reward balance/flow cleanup: found the route sim had flattened to 80/80 wins partly because compile rewards sampled rare modules uniformly with common/uncommon modules, making rare draw engines like `Microservices` routine pickups. `DraftSystem` now uses weighted offer sampling without replacement (`common` > `uncommon` > `rare`), and `Microservices` is marked `unique` so it cannot be compiled repeatedly once owned. Also fixed draft reward readability discovered during browser proofing: desktop compile cards now have enough vertical space for description/meta text, and mobile draft screens scroll internally so all three reward cards plus Cancel are reachable. Verification: 500 compile-offer sample produced rares at 7.1% of slots with `Microservices` in 27/500 offers and no duplicate IDs; owned-Microservices smoke proved 200 compile rolls did not offer it again; draft-aware route sim moved back to boss-shaped pressure at 79/80 victories, 1/80 loss, no pre-boss deaths, `energyDrift: 0`, average Budder 13.26 turns; browser proofs `tmp_asset_contact/draft-weighted-compile-1792x1024.png` and `tmp_asset_contact/draft-weighted-compile-430x932.png` show readable cards with no debt/curse/duplicate offers and no console/page errors; `npm run build`, Cait Momentum black-hole, Phaser no-click-reset, and Technomancy HUD desktop/mobile regressions passed. The stock `develop-web-game` client still fails on unresolved ESM `playwright`.
- Cait Momentum animation proof hardening: found the black-hole verifier only proved a nonblank canvas and did not assert the explicit art direction: orange/gold forward pull, blue backward/reset wake. The stricter verifier now captures a pre-effect baseline and measures changed-pixel color deltas across windup/impact/return frames. That initially exposed a real flow flaw: Cait windup FX was queued behind generic toast/damage/turn FX, so the return wake often played after the UI had already advanced and the return frame was not blue-dominant. Moved Cait Momentum black-hole startup to the `caitAttackWindup` event with only a short local delay, recolored Cait's returning vector blue, and added a high-depth blue reset beam/more wake motes/rings so the backward pull reads through the arena noise. Verification: `tmp_asset_contact/verify-cait-momentum-black-hole.mjs` now asserts forward orange/gold presence, warm-dominant impact, blue-dominant return, changed frames, no console/page errors, and current proof reports return `backwardBlue: 17020` vs warm `12333`; visual frames inspected at `tmp_asset_contact/cait-momentum-black-hole-windup.png`, `tmp_asset_contact/cait-momentum-black-hole-impact.png`, and `tmp_asset_contact/cait-momentum-black-hole-return.png`. `npm run build`, Phaser no-click-reset, and Technomancy HUD desktop/mobile regressions passed. The stock `develop-web-game` client still fails on unresolved ESM `playwright`.
- Cait Momentum turn-banner timing fix: screenshot verification exposed that the body-level `PLAYER TURN` banner could appear over the black-hole payoff/return frames even though combat state had already advanced correctly. `startPlayerTurn()` now supports a visual `phaseDelayMs`, stack resolution delays only the player phase banner, and `main.js` turn banners auto-hide with stale delayed banners guarded by a token. The black-hole verifier now records banner state for windup/impact/return and fails if a visible player banner overlaps those proof frames; it also isolates the target enemy so unrelated enemy FX do not distort the color proof. Strengthened the return wake with a wider blue reset wash/beam/mote lane. Verification: `tmp_asset_contact/verify-cait-momentum-black-hole.mjs` passes with `bannerProof.noPrematurePlayerTurn: true`, return `backwardBlue: 18672` vs warm `10561`, no console/page errors, and inspected frames at `tmp_asset_contact/cait-momentum-black-hole-windup.png`, `tmp_asset_contact/cait-momentum-black-hole-impact.png`, and `tmp_asset_contact/cait-momentum-black-hole-return.png`. `npm run build`, Phaser no-click-reset, and Technomancy HUD desktop/mobile regressions passed. The stock `develop-web-game` client still fails on unresolved ESM `playwright`.
- Cait Momentum overlay/readability cleanup: visual inspection showed the black-hole animation was technically correct but still hidden by combat feedback chrome. During combat, transient toasts now become a compact left-rail feed, cap visible combat toasts, and hide entirely on narrow layouts where the tactical log already carries the stream. The DOM turn banner now uses a combat-phase chip off the battlefield and auto-hides; the Phaser in-canvas phase banner was moved from the arena center to a small upper-left chip so it no longer covers the pull lane. The black-hole verifier now fails if DOM phase banners or toasts overlap the battlefield during windup/impact/return, while keeping the explicit color proof: orange/gold forward pull and blue return. Verification: `tmp_asset_contact/verify-cait-momentum-black-hole.mjs` passes with `bannerProof.noBattlefieldOverlap: true`, `toastOverlayProof.noBattlefieldOverlap: true`, `maxVisibleCombatToasts: 2`, `forwardPullWarmDominant: true`, and `returnBlueDominant: true`; visual frame inspected at `tmp_asset_contact/cait-momentum-black-hole-windup.png`. `npm run build`, Phaser no-click-reset, and Technomancy HUD desktop/mobile regressions passed. The stock `develop-web-game` client still fails on unresolved ESM `playwright`.
- Hero/combat screenshot polish: retuned the Cait bond screen so Asiphyx's focused card aligns farther left with the painted frame, the heart/play control sits left of center on Cait's chest core, and the lower-right Cait Variant panel extends downward with a clipped lower-right corner to cover the baked fake button. Combat desktop now docks the system menu and music controls inside the empty lower-right terminal space instead of overlapping the Battle Report/readout stack.
